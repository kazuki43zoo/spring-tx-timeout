<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.domain.repository.TodoRepository1">
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO todo (title, details, finished) VALUES (#{title}, #{details},
		#{finished})
	</insert>
	<select id="select" resultType="com.example.domain.model.Todo">
		SELECT id, title, details,
		finished FROM todo WHERE id = #{id}
	</select>
	<select id="selectAll" resultType="com.example.domain.model.Todo">
		SELECT id, title, details,
		finished FROM todo
	</select>
	<update id="update">
		UPDATE todo SET title = #{title} WHERE details =
		#{details}
	</update>
	<insert id="insertList" parameterType="list" useGeneratedKeys="true"
		keyProperty="id">
		<foreach collection="list" item="todo" separator=";">
			INSERT
			INTO
			todo (title, details, finished) VALUES (#{todo.title}, #{todo.details},
			#{todo.finished})
		</foreach>
	</insert>

</mapper>